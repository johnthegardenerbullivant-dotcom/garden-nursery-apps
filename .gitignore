# Personal data — never share these
Backups/
*backup*.json
plant-import.json

# OS / editor noise
.DS_Store
Thumbs.db
*.bak
*~

# Anyone who fills in their own real Firebase keys can uncomment the next
# two lines to avoid committing them. (By default the repo ships blank
# placeholders, which are safe to share.)
# garden-management/firebase-config.js
# nursery-management/firebase-config.js
